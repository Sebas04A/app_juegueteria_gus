// Guardar en: lib/src/utils/app_colors.dart

import 'package:flutter/material.dart';

// Definimos una clase para nuestros colores para tener un acceso fácil y centralizado.
class AppColors {
  // Un cian vibrante y divertido como color principal.
  static const Color primary = Color(0xFF00B8D4);
  // Un amarillo ámbar cálido como color de acento.
  static const Color secondary = Color(0xFFFFC107);
  // Un fondo muy claro para que los productos resalten.
  static const Color background = Color(0xFFF7F9FA);
  // Blanco puro para las superficies de las tarjetas.
  static const Color surface = Colors.white;
  // Colores de texto para una buena legibilidad.
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
}
